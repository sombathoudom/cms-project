APP_NAME=SAFE_DEFAULT=CMS Platform          # REQUIRED
APP_ENV=SAFE_DEFAULT=local                  # REQUIRED
APP_KEY=SECRET=<base64-key>                 # REQUIRED # DO NOT COMMIT
APP_DEBUG=SAFE_DEFAULT=false                # REQUIRED
APP_URL=SAFE_DEFAULT=http://localhost       # REQUIRED
APP_TIMEZONE=SAFE_DEFAULT=UTC               # REQUIRED
APP_LOCALE=SAFE_DEFAULT=en                  # REQUIRED
APP_FALLBACK_LOCALE=SAFE_DEFAULT=en         # REQUIRED
APP_FAKER_LOCALE=SAFE_DEFAULT=en_US         # OPTIONAL

LOG_CHANNEL=SAFE_DEFAULT=stack              # REQUIRED
LOG_STACK_CHANNELS=SAFE_DEFAULT=json        # REQUIRED
LOG_LEVEL=SAFE_DEFAULT=info                 # REQUIRED

DB_CONNECTION=SAFE_DEFAULT=mysql            # REQUIRED
DB_HOST=SAFE_DEFAULT=db                     # REQUIRED
DB_PORT=SAFE_DEFAULT=3306                   # REQUIRED
DB_DATABASE=SAFE_DEFAULT=cms                # REQUIRED
DB_USERNAME=SECRET=<db-user>                # REQUIRED # DO NOT COMMIT
DB_PASSWORD=SECRET=<db-pass>                # REQUIRED # DO NOT COMMIT

CACHE_STORE=SAFE_DEFAULT=redis              # REQUIRED
CACHE_PREFIX=SAFE_DEFAULT=cms_              # OPTIONAL
QUEUE_CONNECTION=SAFE_DEFAULT=redis         # REQUIRED
SESSION_DRIVER=SAFE_DEFAULT=redis           # REQUIRED
SESSION_LIFETIME=SAFE_DEFAULT=120           # REQUIRED
SESSION_DOMAIN=SAFE_DEFAULT=localhost       # OPTIONAL

REDIS_CLIENT=SAFE_DEFAULT=phpredis          # REQUIRED
REDIS_HOST=SAFE_DEFAULT=redis               # REQUIRED
REDIS_PASSWORD=SECRET=null                  # REQUIRED # DO NOT COMMIT
REDIS_PORT=SAFE_DEFAULT=6379                # REQUIRED

FILESYSTEM_DISK=SAFE_DEFAULT=public         # REQUIRED
S3_KEY=SECRET=<aws-key>                     # OPTIONAL # DO NOT COMMIT
S3_SECRET=SECRET=<aws-secret>               # OPTIONAL # DO NOT COMMIT
S3_REGION=SAFE_DEFAULT=us-east-1            # OPTIONAL
S3_BUCKET=SAFE_DEFAULT=cms-media            # OPTIONAL

MEILISEARCH_HOST=SAFE_DEFAULT=http://meilisearch:7700   # REQUIRED
MEILISEARCH_KEY=SECRET=<meili-key>                      # REQUIRED # DO NOT COMMIT
SCOUT_DRIVER=SAFE_DEFAULT=database                      # REQUIRED

SANCTUM_STATEFUL_DOMAINS=SAFE_DEFAULT=localhost        # REQUIRED
SESSION_SECURE_COOKIE=SAFE_DEFAULT=false               # OPTIONAL
QUEUE_SUPERVISOR_SLEEP=SAFE_DEFAULT=3                  # OPTIONAL

BROADCAST_DRIVER=SAFE_DEFAULT=log          # OPTIONAL
MAIL_MAILER=SAFE_DEFAULT=log               # OPTIONAL
MAIL_FROM_ADDRESS=SAFE_DEFAULT=admin@example.com  # OPTIONAL
MAIL_FROM_NAME=SAFE_DEFAULT="${APP_NAME}"         # OPTIONAL
